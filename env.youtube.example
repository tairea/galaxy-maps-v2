# Example environment variables for the YouTube MCP integration
# Copy these into your local .env and fill the values as needed.
# Required: API key that the youtube-mcp-server will use.
VITE_YOUTUBE_API_KEY=""

# Optional: override the command used to launch the MCP server locally.
# VITE_YOUTUBE_MCP_COMMAND="npx -y zubeid-youtube-mcp-server"

# Optional: point to a remote Streamable HTTP MCP server instead of spawning locally.
# VITE_YOUTUBE_MCP_SERVER_URL=""

# Optional: choose a default transcript language (defaults to en when omitted).
# VITE_YOUTUBE_TRANSCRIPT_LANG="en"
