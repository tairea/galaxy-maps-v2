# Install & Prime

## Read and Execute
.claude/commands/prime.md

## Run
- Install dependencies: `npm install`
- Install Firebase Functions dependencies: `cd functions && npm install && cd ..`
- Verify .env file exists (or copy from .env.production if needed)

## Report
Output the work you've just done in a concise bullet point list.