# ==============================================================================
# Galaxy Maps Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control
# ==============================================================================

# ------------------------------------------------------------------------------
# Firebase Configuration
# ------------------------------------------------------------------------------
# Get these values from your Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Select your project (or create a new one)
# 3. Go to Project Settings → General
# 4. Scroll to "Your apps" section
# 5. Click on the web app (or create one if needed)
# 6. Copy the config values to the variables below

# Firebase Web API Key
VITE_FIREBASE_API_KEY=your-firebase-api-key-here

# Firebase Auth Domain (usually your-project-id.firebaseapp.com)
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# Firebase Realtime Database URL (usually https://your-project-id-default-rtdb.firebaseio.com)
VITE_FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com

# Firebase Project ID
VITE_FIREBASE_PROJECT_ID=your-project-id

# Firebase Storage Bucket (usually your-project-id.appspot.com)
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Firebase Cloud Messaging Sender ID
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id

# Firebase App ID
VITE_FIREBASE_APP_ID=1:your-app-id:web:your-app-identifier

# Firebase Analytics Measurement ID (optional)
VITE_FIREBASE_MEASUREMENT_ID=G-YOUR-MEASUREMENT-ID

# ------------------------------------------------------------------------------
# Third-Party API Keys
# ------------------------------------------------------------------------------

# Veracity LRS Secret for xAPI learning analytics
# Format: username:password
VITE_VERACITY_LRS_SECRET=your-lrs-username:your-lrs-password

# OpenAI API Key for AI-powered features (galaxy generation, content creation)
# Get this from https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here

# ------------------------------------------------------------------------------
# Firebase Emulator Configuration (Optional - for local development)
# ------------------------------------------------------------------------------
# Uncomment and configure these when using Firebase emulators for local testing
# Run: npm run dev:emulator
#
# IMPORTANT: Use "localhost" (not "127.0.0.1") to avoid CORS errors when the
# frontend runs on localhost:5173. Browsers treat localhost and 127.0.0.1 as
# different origins for CORS purposes.

# Enable Firebase emulators
# VITE_USE_EMULATOR=true

# Firestore Emulator Host (default: localhost:8080)
# VITE_FIRESTORE_EMULATOR_HOST=localhost:8080

# Auth Emulator URL (default: http://localhost:9099)
# VITE_FIREBASE_AUTH_EMULATOR=http://localhost:9099

# Functions Emulator Host (default: localhost:5001)
# VITE_FUNCTIONS_EMULATOR_HOST=localhost:5001

# Storage Emulator Host (default: localhost:9199)
# VITE_STORAGE_EMULATOR_HOST=localhost:9199

# Realtime Database Emulator Host (default: localhost:9000)
# VITE_DATABASE_EMULATOR_HOST=localhost:9000

# ==============================================================================
# Setup Instructions
# ==============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Create a Firebase project at https://console.firebase.google.com/
# 3. Enable the following Firebase services:
#    - Authentication (Email/Password provider)
#    - Firestore Database
#    - Realtime Database
#    - Cloud Storage
#    - Cloud Functions
# 4. Get your Firebase config and fill in the VITE_FIREBASE_* variables above
# 5. Get an OpenAI API key from https://platform.openai.com/api-keys
# 6. (Optional) Set up Veracity LRS credentials for learning analytics
# 7. Run: npm install
# 8. Run: npm run dev
# ==============================================================================
