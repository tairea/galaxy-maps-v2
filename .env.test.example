# Playwright E2E Test Credentials
# Copy this file to .env.test and fill in your actual test credentials
# The .env.test file is gitignored and will not be committed

# Test user email for E2E tests
E2E_TEST_EMAIL=your-test-email@example.com

# Test user password for E2E tests
E2E_TEST_PASSWORD=your-test-password

# Base URL for E2E tests (optional, defaults to http://127.0.0.1:5173)
BASE_URL=http://127.0.0.1:5173

# Firebase Project ID for emulator tests
FIREBASE_PROJECT_ID=your-project-id

# Firebase Emulator Configuration
# All emulators use 127.0.0.1 to match BASE_URL and avoid CORS issues
# IMPORTANT: Use 127.0.0.1 (not localhost) to match BASE_URL origin
VITE_USE_EMULATOR=true
VITE_FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
VITE_FIREBASE_AUTH_EMULATOR=http://127.0.0.1:9099
VITE_FUNCTIONS_EMULATOR_HOST=127.0.0.1:5001
VITE_STORAGE_EMULATOR_HOST=127.0.0.1:9199
VITE_DATABASE_EMULATOR_HOST=127.0.0.1:9000
