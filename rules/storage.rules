rules_version = '2';

// Storage security rules for emulator testing
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow authenticated users to read and write during emulator testing
      allow read, write: if request.auth != null;
    }
  }
}
